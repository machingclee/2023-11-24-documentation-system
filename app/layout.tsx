import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import '@fontsource/roboto/300.css';
import '@fontsource/roboto/400.css';
import '@fontsource/roboto/500.css';
import '@fontsource/roboto/700.css';
import NavBar from './NavBar';
import Box from '@mui/material/Box';
import Providers from './ReduxProviders';
import AuthProvider from './auth/Provider';
import Spacer from '../component/Spacer';
import MathjaxProvider from '../component/MathjaxProvider';
import Head from 'next/head'

const inter = Inter({
    subsets: ['latin'],
    variable: "--font-inter"
})

export const metadata: Metadata = {
    title: 'Issue Apps',
    description: 'Generated by create next app',
}

export default function RootLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (

        <html lang="en">
            <Head>
                <script type="text/javascript" src="http://cdn.mathjax.org/mathjax/latest/MathJax.js?config=TeX-AMS-MML_HTMLorMML"> </script >
            </Head>
            <body style={{ height: "100vh", padding: 0, margin: 0 }}>
                <MathjaxProvider>
                    <AuthProvider>
                        <Providers>
                            <Box sx={{
                                display: "flex",
                                height: "100vh",
                                overflowY: "hidden",

                                "& .MuiButtonBase-root": {
                                    textTransform: "none"
                                }
                            }}>
                                <div style={{ height: "100%", width: 300 }}>
                                    <div style={{ backgroundColor: "#4e5346", height: 43, }}></div>
                                    <div style={{ height: "calc(100vh - 43px)", overflowY: "scroll", padding: 20 }}>
                                        test
                                    </div>
                                </div>
                                <div style={{ height: "100%", flex: 1, }}>
                                    <NavBar />
                                    <div style={{ height: "calc(100vh - 63px)", backgroundColor: "white", overflowY: "scroll", paddingTop: 20 }}>
                                        <div className={inter.variable}>{children}</div>
                                    </div>
                                </div>
                            </Box>
                        </Providers>
                    </AuthProvider>
                </MathjaxProvider>
            </body>
        </html>
    )
}
